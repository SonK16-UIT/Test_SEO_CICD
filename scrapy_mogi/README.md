# Scrapy Mogi Pipeline

This package contains a Scrapy + Playwright crawler for `mogi.vn/mua-nha-dat`.

Run:

```bash
cd scrapy_mogi
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
scrapy crawl mogi_listings
```
